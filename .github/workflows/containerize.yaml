# name of the workflow.
# this is optional.
name: Containerize

on:
  push:
    branches:
      - develop # Set to '**' for testing on all branches
  workflow_dispatch: # Allow manual triggering

env:
  REGISTRY_GH: ghcr.io

#This also includes how I upload it to our forgejo instance, feel free to add or remove it to the github container registry
jobs:
  deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Log into GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY_GH }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push backend
        uses: docker/build-push-action@v6
        with:
          context: .
          file: backend/Dockerfile
          push: true
          tags: |
            ${{ env.REGISTRY_GH }}/${{ github.repository_owner }}/cvt-simulator-backend:latest
            ${{ env.REGISTRY_GH }}/${{ github.repository_owner }}/cvt-simulator-backend:${{ github.sha }}

      - name: Build and push frontend
        uses: docker/build-push-action@v6
        with:
          context: .
          file: frontend/Dockerfile
          push: true
          tags: |
            ${{ env.REGISTRY_GH }}/${{ github.repository_owner }}/cvt-simulator-frontend:latest
            ${{ env.REGISTRY_GH }}/${{ github.repository_owner }}/cvt-simulator-frontend:${{ github.sha }}